version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: dockerfile.production
    env_file:
      - ".env prod"
    command: uvicorn main_app.main:app --host 0.0.0.0 --port 80
    ports:
      - "80:80"
    restart: unless-stopped
